# assigning values to variables.
a <- 3
b <- 2
c <- -1

# solving the quadratic equations
(-b+sqrt(b^2-4*a*c))/(2*a)
(-b-sqrt(b^2-4*a*c))/(2*a)

x # if runnning this line only, it'll show "Error: object 'x' not found"
ls # Error in eval(ei,envire) tells the code of Is
log(8)
log(a)
# exponential
exp(1) 
log(2.718282)
log(exp(1))
help("log") ## tells in detail about the parameter in it
args("log") ## shows which arguments ther are in it
log(8, base = 2) ## without x =, it'll just assume that it is following the order in help() 
log(x = 8, base = 2) 
2^3
help("+")
?"+"
data()
CO2
pi
Inf

3x^2 + 2x - 1 
